-- j'ai compilé mon projet sur linux par la commande : gcc main.c -lSDL -lSDL_image -o run 
-- pour executer run il suffit d'utiliser la commande : ./run sur terminal 
-- j'ai remplie la MAP manuellement  vous pouvez modifier les positions des feu et les obstacle par changement des coordonnées dans la fonction load_MAP() .
et merci ..